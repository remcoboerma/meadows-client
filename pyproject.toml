[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "meadows-client"
version = "0.1.0"
description = "MEADOWS client transport: connect, reconnect, handshake, JWT. Shared by bots and future non-browser clients."
readme = "README.md"
requires-python = ">=3.12"
license-expression = "MIT"
authors = [{ name = "MEADOWS" }]
dependencies = [
    "meadows-protocol",
    "aiohttp>=3.9.0",
    "python-socketio>=5.11.0",
    "pyjwt>=2.12.0",
]

[project.optional-dependencies]
dev = [
    "hatch",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=5.0.0",
    "ruff==0.14.9",
]

[tool.hatch.build.targets.wheel]
packages = ["src/meadows"]

[tool.uv.sources]
meadows-protocol = { path = "../meadows-protocol", editable = true }

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["F", "E", "W", "Q", "A", "SIM", "ARG", "PTH", "RUF", "C90", "N", "YTT"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.vommit]
allow_breaking_bang = true
allow_breaking_footer = true
prerelease_token = "rc"
confirm = true

[tool.vommit.git]
enabled = true
origin = "origin"
branch = "main"
on_wrong_branch = "error"
tag_format = "v{version}"
commit_format = "{version}"

[tool.vommit.changelog]
enabled = true
file = "CHANGELOG.md"
include_prereleases = false
placeholder = "<!-- next-version-placeholder -->"
placeholder_regex = "<auto>"
entry_title_format = "## v{version} ({date:%Y-%m-%d})"

[tool.vommit.changelog.levels]
break = "Breaking Change{s}"
feat = "Feature{s}"
fix = "Fix{es}"
perf = "Performance"
docs = "Documentation"

[tool.vommit.pypi]
enabled = true
use_keyring = true

[tool.vommit.commands]
clean = "rm -rf ./dist"
build = "uv build"
publish = "../pypi-publish"
post_publish = ""

[tool.vommit.version_bump_map]
break = "major"
feat = "minor"
fix = "patch"
perf = "patch"
